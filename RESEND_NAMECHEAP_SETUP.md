# 📧 Настройка Resend с доменом на Namecheap

## Пошаговая инструкция для Namecheap

### Шаг 1: Получите DNS записи в Resend

1. Зайдите на [resend.com](https://resend.com) и войдите
2. Перейдите: **Domains** → **Add Domain**
3. Введите ваш домен (например: `renohacks.com`)
4. Нажмите **Add**
5. **Скопируйте все DNS записи**, которые покажет Resend:
   - SPF (TXT запись)
   - DKIM (обычно 2-3 CNAME записи)
   - DMARC (опционально)

### Шаг 2: Добавление DNS записей в Namecheap

1. Откройте [панель Namecheap](https://ap.www.namecheap.com/domains/list/)
2. Найдите ваш домен в списке
3. Нажмите **"Manage"** (Управление) справа от домена
4. Перейдите на вкладку **"Advanced DNS"** (Расширенные DNS)

### Шаг 3: Добавление SPF записи (TXT)

1. В разделе **"Host Records"** нажмите **"Add New Record"**
2. Выберите тип: **TXT Record**
3. Заполните:
   - **Host**: `@` (или оставьте пустым)
   - **Value**: `v=spf1 include:resend.com ~all` (или значение из Resend)
   - **TTL**: `Automatic` (или `30 min`)
4. Нажмите **✓** (галочка) для сохранения

### Шаг 4: Добавление DKIM записей (CNAME)

Resend покажет обычно 2-3 CNAME записи. Для каждой:

1. Нажмите **"Add New Record"**
2. Выберите тип: **CNAME Record**
3. Заполните (примеры из Resend):
   - **Host**: `resend._domainkey` (или как показано в Resend)
   - **Value**: `[значение из Resend]` (например, `xxx.dkim.resend.com`)
   - **TTL**: `Automatic`
4. Нажмите **✓** для сохранения

**Повторите для всех DKIM записей**, которые показал Resend.

### Шаг 5: Добавление DMARC (опционально, но рекомендуется)

1. Нажмите **"Add New Record"**
2. Выберите тип: **TXT Record**
3. Заполните:
   - **Host**: `_dmarc`
   - **Value**: `v=DMARC1; p=none; rua=mailto:your-email@renohacks.com`
     - Замените `your-email@renohacks.com` на ваш реальный email
   - **TTL**: `Automatic`
4. Нажмите **✓** для сохранения

### Шаг 6: Проверка в Resend

1. Подождите 5-15 минут (DNS записи распространяются)
2. Вернитесь в Resend: **Domains**
3. Нажмите **"Verify"** рядом с вашим доменом
4. Или подождите автоматической проверки
5. Статус должен стать **"Verified"** ✅ (зеленая галочка)

### Шаг 7: Настройка переменных окружения

#### Локально (`.env.local`):

```env
RESEND_API_KEY=re_ваш-ключ-из-resend
RESEND_FROM_EMAIL=noreply@renohacks.com
```

**Где получить API ключ:**
1. В Resend: **API Keys** → **Create API Key**
2. Имя: `Renohacks Production`
3. Права: **Sending access**
4. Скопируйте ключ (формат: `re_...`)

#### В Vercel:

1. Откройте проект на [vercel.com](https://vercel.com)
2. **Settings** → **Environment Variables**
3. Добавьте:
   - `RESEND_API_KEY` = `re_ваш-ключ`
   - `RESEND_FROM_EMAIL` = `noreply@renohacks.com`
4. Выберите **Environment**: Production, Preview, Development
5. **Save**

### Шаг 8: Перезапуск

- **Локально**: Перезапустите `npm run dev`
- **Vercel**: Redeploy последний деплой

---

## 📸 Примеры записей в Namecheap

### SPF (TXT):
```
Type: TXT Record
Host: @
Value: v=spf1 include:resend.com ~all
TTL: Automatic
```

### DKIM (CNAME) - пример 1:
```
Type: CNAME Record
Host: resend._domainkey
Value: xxx.dkim.resend.com
TTL: Automatic
```

### DKIM (CNAME) - пример 2:
```
Type: CNAME Record
Host: resend2._domainkey
Value: yyy.dkim.resend.com
TTL: Automatic
```

### DMARC (TXT):
```
Type: TXT Record
Host: _dmarc
Value: v=DMARC1; p=none; rua=mailto:admin@renohacks.com
TTL: Automatic
```

---

## ⚠️ Важные моменты

1. **Host поле**:
   - Для корневого домена используйте `@` или оставьте пустым
   - Для поддоменов (например, `_dmarc`) введите как есть

2. **TTL**: Рекомендуется `Automatic` или `30 min`

3. **Время распространения**: DNS записи могут распространяться от 5 минут до 48 часов, но обычно 5-15 минут

4. **Проверка**: После добавления записей подождите 10-15 минут перед проверкой в Resend

---

## 🔍 Проверка DNS записей

После добавления можно проверить через:

1. **MXToolbox**: https://mxtoolbox.com/spf.aspx
   - Введите ваш домен
   - Проверьте SPF и DKIM записи

2. **DNS Checker**: https://dnschecker.org
   - Введите домен и тип записи (TXT, CNAME)

3. **Через терминал** (если установлен):
   ```bash
   nslookup -type=TXT renohacks.com
   nslookup -type=CNAME resend._domainkey.renohacks.com
   ```

---

## ❓ Проблемы и решения

### Домен не верифицируется в Resend

1. **Проверьте все записи добавлены** - SPF, все DKIM, DMARC
2. **Проверьте формат** - нет ли опечаток в значениях
3. **Подождите еще 15-30 минут** - DNS может распространяться медленно
4. **Проверьте через MXToolbox** - видны ли записи

### Письма не приходят

1. **Проверьте статус домена** - должен быть "Verified" в Resend
2. **Проверьте `RESEND_FROM_EMAIL`** - должен быть с вашего домена
3. **Проверьте консоль сервера** - там будут ошибки
4. **Проверьте спам** - иногда письма попадают туда

### Ошибка "Domain not verified"

- Убедитесь, что домен верифицирован в Resend (зеленая галочка)
- Проверьте DNS записи через MXToolbox
- Убедитесь, что прошло достаточно времени (15-30 минут)

---

## ✅ Готово!

После настройки все письма будут отправляться с вашего домена `noreply@renohacks.com`, что значительно улучшит доставляемость!

