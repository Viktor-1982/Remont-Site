# Руководство по Session-Based Authentication

## ✅ Что было сделано

Реализована безопасная система аутентификации администратора на основе HTTP-only cookies вместо публичных токенов.

### Преимущества новой системы:

1. **Безопасность**: Токены сессии хранятся в HTTP-only cookies, недоступны через JavaScript
2. **Защита от XSS**: Даже если XSS-уязвимость найдена, злоумышленник не сможет украсть токен
3. **Автоматическое управление**: Сессии автоматически истекают через 8 часов
4. **Простота**: Не нужно передавать токены в заголовках запросов

## 📁 Структура файлов

```
src/
├── lib/
│   ├── admin-session.ts       # Управление сессиями (создание, валидация, удаление)
│   └── admin-auth.ts          # Утилита для проверки аутентификации в API routes
└── app/
    ├── api/
    │   └── admin/
    │       └── auth/
    │           ├── login/route.ts    # POST: вход и создание сессии
    │           ├── logout/route.ts   # POST: выход и удаление сессии
    │           └── session/route.ts  # GET: проверка текущей сессии
    └── admin/
        └── page.tsx            # Обновленная админ-панель
```

## 🔧 Настройка

### Переменные окружения

В `.env.local` (для разработки) или в настройках хостинга (для продакшена):

```bash
# ✅ Обязательно: пароль администратора
# Поддержка нескольких паролей через запятую
ADMIN_PASSWORD=ваш-сильный-пароль
# или
ADMIN_PASSWORDS=пароль1,пароль2,пароль3

# ⚠️ БОЛЬШЕ НЕ НУЖНО:
# NEXT_PUBLIC_ADMIN_TOKEN - удалите эту переменную!
# ADMIN_TOKEN - больше не используется
```

### Рекомендации по паролям:

- Минимум 16 символов
- Используйте случайные строки (например, `openssl rand -base64 24`)
- Не используйте дефолтные пароли (`admin123`)
- В продакшене используйте разные пароли для разных окружений

## 🔐 Как это работает

### 1. Вход в систему

```typescript
// Клиент отправляет пароль на сервер
POST /api/admin/auth/login
Body: { password: "ваш-пароль" }

// Сервер:
// 1. Проверяет пароль
// 2. Создает сессию (UUID токен)
// 3. Устанавливает HTTP-only cookie с токеном
// 4. Возвращает success: true

// Cookie установлена автоматически браузером
// admin_session=<uuid>; HttpOnly; Secure; SameSite=Lax
```

### 2. Использование сессии

```typescript
// Все API запросы автоматически включают cookie
fetch("/api/admin/articles", {
    credentials: "include" // ✅ Важно!
})

// Сервер:
// 1. Читает токен из cookie
// 2. Проверяет валидность сессии
// 3. Возвращает данные или 401 Unauthorized
```

### 3. Выход из системы

```typescript
// Клиент вызывает logout endpoint
POST /api/admin/auth/logout

// Сервер:
// 1. Удаляет сессию из хранилища
// 2. Удаляет cookie
// 3. Возвращает success: true
```

## 🛠 API Endpoints

### POST /api/admin/auth/login

**Запрос:**
```json
{
  "password": "ваш-пароль"
}
```

**Ответ (успех):**
```json
{
  "success": true
}
```

**Ответ (ошибка):**
```json
{
  "error": "Invalid password"
}
```

### POST /api/admin/auth/logout

**Запрос:** (без body)

**Ответ:**
```json
{
  "success": true
}
```

### GET /api/admin/auth/session

**Запрос:** (без параметров)

**Ответ:**
```json
{
  "authenticated": true
}
```

## 📝 Использование в API Routes

Все admin API routes теперь используют `checkAdminAuth`:

```typescript
import { checkAdminAuth } from "@/lib/admin-auth"

export async function GET(req: NextRequest) {
    // ✅ Проверка через HTTP-only cookie
    if (!(await checkAdminAuth(req))) {
        return NextResponse.json({ error: "Unauthorized" }, { status: 401 })
    }
    
    // Ваш код...
}
```

## ⚠️ Важные моменты

### 1. Credentials в Fetch

**ВАЖНО**: Все fetch запросы должны включать `credentials: "include"`:

```typescript
fetch("/api/admin/articles", {
    credentials: "include" // ✅ Обязательно!
})
```

Без этого cookie не будут отправляться на сервер!

### 2. HTTP vs HTTPS

- В разработке (`localhost`): cookie работает по HTTP
- В продакшене: cookie автоматически работает только по HTTPS (флаг `secure: true`)

### 3. Хранение сессий

**Текущая реализация**: Сессии хранятся в памяти (Map)

**Ограничения:**
- При перезапуске сервера все сессии теряются
- Не работает в multi-server окружении

**Для продакшена рекомендуется:**
- Redis для хранения сессий
- База данных (PostgreSQL, MongoDB)
- Или JWT токены с коротким сроком жизни

Пример интеграции с Redis (для будущей реализации):

```typescript
// src/lib/admin-session-redis.ts
import { Redis } from '@upstash/redis'

const redis = new Redis({ url: process.env.REDIS_URL })

export async function createSession(): Promise<string> {
    const token = crypto.randomUUID()
    const expiresAt = Date.now() + SESSION_DURATION
    
    await redis.setex(`admin_session:${token}`, expiresAt, "1")
    return token
}

export async function validateSession(token: string): Promise<boolean> {
    const exists = await redis.exists(`admin_session:${token}`)
    return exists === 1
}
```

## 🔒 Безопасность

### Защита от атак:

1. **XSS**: HTTP-only cookie недоступна через JavaScript
2. **CSRF**: SameSite=Lax защищает от большинства CSRF атак
3. **Man-in-the-Middle**: Secure flag требует HTTPS в продакшене
4. **Brute-force**: Задержка 2 секунды при неудачном входе

### Дополнительные меры (рекомендуется):

1. **Rate Limiting**: Ограничьте количество попыток входа
2. **IP Whitelist**: Разрешите доступ только с определенных IP
3. **2FA**: Добавьте двухфакторную аутентификацию
4. **Логирование**: Логируйте все попытки входа (успешные и неудачные)

## 🚀 Миграция

### Что изменилось:

1. ✅ Удалено использование `NEXT_PUBLIC_ADMIN_TOKEN`
2. ✅ Удалено использование `ADMIN_TOKEN` в заголовках
3. ✅ Добавлена система сессий с HTTP-only cookies
4. ✅ Все API routes обновлены для использования cookies

### Совместимость:

- Старые запросы с Bearer токенами больше не работают
- Нужно войти заново через новый API endpoint
- Сессии автоматически проверяются при загрузке админ-панели

## ❓ Частые вопросы

**Q: Нужно ли менять пароль в .env.local?**
A: Нет, используется тот же `ADMIN_PASSWORD`. Просто удалите `NEXT_PUBLIC_ADMIN_TOKEN`.

**Q: Что делать если сессия истекла?**
A: Просто войдите заново. Сессия автоматически обновляется при активности.

**Q: Можно ли использовать несколько браузеров?**
A: Да, каждая вкладка создает свою сессию.

**Q: Безопасно ли хранить сессии в памяти?**
A: Для разработки - да. Для продакшена рекомендуется Redis или база данных.

## 📚 Дополнительные ресурсы

- [Next.js Cookies](https://nextjs.org/docs/app/api-reference/functions/cookies)
- [HTTP-only Cookies Security](https://owasp.org/www-community/HttpOnly)
- [Session Management Best Practices](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)

