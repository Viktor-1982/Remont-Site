# Настройка админ-панели

## Переменные окружения

Создайте файл `.env.local` в корне проекта и добавьте:

```env
# Пароль для входа в админ-панель
NEXT_PUBLIC_ADMIN_PASSWORD=ваш-секретный-пароль

# Токен для API (используется серверными маршрутами)
ADMIN_TOKEN=ваш-секретный-токен

# ==========================================
# Настройки для автоматического коммита в Git (для Vercel)
# ==========================================
# Если хотите, чтобы фото автоматически коммитились в Git:

# Включить автокоммит (true/false)
NEXT_PUBLIC_USE_GIT_AUTO_COMMIT=false

# GitHub токен с правами на запись в репозиторий
# Создайте здесь: https://github.com/settings/tokens
# Нужны права: repo (полный доступ к репозиторию)
GITHUB_TOKEN=ghp_ваш-токен

# Репозиторий в формате owner/repo (например: username/repair-blog)
GITHUB_REPO=username/repair-blog

# Настройки Git (опционально)
GIT_USER="Your Name"
GIT_EMAIL="your@email.com"
```

⚠️ **Важно**: 
- По умолчанию используется токен `your-secret-token` (для разработки)
- Для продакшена **обязательно** измените оба значения на сложные строки
- `NEXT_PUBLIC_ADMIN_PASSWORD` доступен на клиенте, поэтому используйте сложный пароль
- Для продакшена рекомендуется использовать более безопасную систему аутентификации (например, NextAuth.js)

## Использование админ-панели

1. Откройте `http://localhost:3000/admin`
2. Введите пароль (из `NEXT_PUBLIC_ADMIN_PASSWORD`)
3. Вы увидите список всех статей

## Как работать с изображениями

### Вставка изображения в нужное место:

1. Откройте статью для редактирования (или создайте новую)
2. В поле "Содержание статьи" поставьте курсор в то место, где хотите вставить изображение
3. Нажмите кнопку **"Вставить фото"**
4. Выберите изображение - оно автоматически:
   - Загрузится в папку `/public/images/[slug-статьи]/`
   - Вставит markdown код `![Описание изображения](/images/путь/файл.png)` в позицию курсора
5. Отредактируйте описание изображения (замените "Описание изображения" на нужный текст)

### Пример:

Если курсор стоит здесь:
```
Текст до изображения.
```
(курсор здесь)

После загрузки изображения получится:
```
Текст до изображения.

![Описание изображения](/images/название-статьи/название-статьи-1234567890.png)

```
(курсор здесь, можно продолжать писать)

## Структура папок для изображений

Изображения сохраняются автоматически в:
- `/public/images/[slug-статьи]/` - для каждой статьи своя папка
- Имя файла: `[slug-статьи]-[timestamp].[расширение]`

Например, для статьи со slug `remont-vannoy`:
- `/public/images/remont-vannoy/remont-vannoy-1234567890.png`

## ⚠️ Важно для Vercel/Netlify

Если ваш сайт на **Vercel** или **Netlify**, фото загруженные через админку **пропадут** после перезапуска!

### Решение: Автоматический коммит в Git

**📖 Подробная инструкция:** См. файл `GITHUB_SETUP.md`

Кратко:
1. Создайте GitHub токен на https://github.com/settings/tokens (права: `repo`)
2. Настройте в Vercel переменные:
   - `GITHUB_TOKEN` = ваш токен
   - `GITHUB_REPO` = `username/repair-blog`
   - `NEXT_PUBLIC_USE_GIT_AUTO_COMMIT` = `true`
3. Перезапустите деплой
4. Готово! Фото автоматически попадают в Git
5. Локально делайте `git pull` чтобы получить фото

### Альтернатива: Загружать фото только локально

Если не хотите настраивать GitHub токен:
- Загружайте фото только локально (в dev режиме)
- Коммитьте их вручную: `git add public/images/ && git commit -m "Add images"`
- Пушите в репозиторий: `git push`

## Создание новой статьи

1. Нажмите "Новая статья"
2. Заполните все поля:
   - **Заголовок** - обязательное поле
   - **Slug** - URL часть (если не указан, генерируется автоматически из заголовка)
   - **Описание** - обязательное поле
   - **Дата** - по умолчанию сегодняшняя
   - **Язык** - ru или en
   - **Теги** - через запятую
   - **Ключевые слова** - через запятую
   - **Обложка** - путь к изображению обложки
3. Напишите содержание в формате MDX
4. Сохраните статью

## Редактирование существующей статьи

1. Выберите статью из списка слева
2. Отредактируйте нужные поля
3. Для вставки изображений используйте курсор в нужном месте текста
4. Сохраните изменения

## Формат изображений в MDX

После загрузки изображения в текст вставляется код:
```markdown
![Описание изображения](/images/название-статьи/файл.png)
```

Вы можете редактировать:
- Описание (альтернативный текст для изображения)
- Путь к изображению (если нужно изменить)

## Примечания

- После сохранения статьи нужно перезапустить `npm run dev` чтобы Contentlayer пересобрал статьи
- Или дождаться автоматического обновления Contentlayer в dev режиме
- Изображения доступны сразу после загрузки

