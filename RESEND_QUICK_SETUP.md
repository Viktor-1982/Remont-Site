# 🚀 Быстрая настройка Resend за 5 минут

## Шаг 1: Регистрация (2 минуты)

1. Откройте [resend.com](https://resend.com)
2. Нажмите **"Sign Up"** (можно через GitHub - быстрее)
3. Подтвердите email (проверьте почту)

## Шаг 2: Создание API ключа (1 минута)

1. В дашборде Resend нажмите **"API Keys"** в меню слева
2. Нажмите **"Create API Key"**
3. Дайте имя: `Renohacks Newsletter`
4. Выберите права: **"Sending access"** ✅
5. Нажмите **"Add"**
6. **ВАЖНО**: Скопируйте ключ сразу! Он показывается только один раз
   - Формат: `re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

## Шаг 3: Настройка локально (1 минута)

1. В корне проекта создайте файл `.env.local` (если его нет)
2. Добавьте туда:

```env
RESEND_API_KEY=re_ваш-ключ-из-шага-2
RESEND_FROM_EMAIL=onboarding@resend.dev
```

**Пример:**
```env
RESEND_API_KEY=re_abc123xyz789
RESEND_FROM_EMAIL=onboarding@resend.dev
```

## Шаг 4: Перезапуск (30 секунд)

1. Остановите dev сервер (Ctrl+C)
2. Запустите снова:
```bash
npm run dev
```

## Шаг 5: Тест (30 секунд)

1. Откройте сайт: `http://localhost:3000`
2. Подпишитесь на рассылку (введите свой email)
3. Проверьте почту - письмо должно прийти в течение 1-2 минут

---

## ✅ Готово!

Теперь письма будут отправляться локально.

---

## 🔧 Настройка для продакшена (Vercel)

### Шаг 1: Добавьте переменные в Vercel

1. Откройте [vercel.com](https://vercel.com) → ваш проект
2. Перейдите: **Settings** → **Environment Variables**
3. Добавьте две переменные:

| Name | Value |
|------|-------|
| `RESEND_API_KEY` | `re_ваш-ключ` |
| `RESEND_FROM_EMAIL` | `onboarding@resend.dev` (или ваш домен) |

4. Выберите **Environment**: `Production`, `Preview`, `Development` (или все)
5. Нажмите **Save**

### Шаг 2: Перезапустите деплой

1. В Vercel перейдите в **Deployments**
2. Нажмите **"..."** на последнем деплое
3. Выберите **"Redeploy"**

Готово! Теперь письма будут отправляться и на продакшене.

---

## 📧 Настройка своего домена (опционально, но рекомендуется)

### Зачем?
- Письма будут приходить с вашего домена (например, `noreply@renohacks.com`)
- Лучше доставляемость (меньше попадает в спам)
- Профессиональный вид

### Как настроить:

1. В Resend дашборде: **Domains** → **Add Domain**
2. Введите домен: `renohacks.com`
3. Resend покажет DNS записи, которые нужно добавить:
   - SPF запись
   - DKIM записи (2-3 записи)
   - DMARC (опционально)
4. Добавьте эти записи в настройки DNS вашего домена
5. Подождите проверки (обычно 5-15 минут)
6. После проверки измените `RESEND_FROM_EMAIL`:
   ```env
   RESEND_FROM_EMAIL=noreply@renohacks.com
   ```

---

## ❓ Частые проблемы

### Письма не приходят

1. **Проверьте консоль сервера** - там будут ошибки
2. **Проверьте `.env.local`** - правильно ли указан ключ?
3. **Перезапустили сервер?** - переменные окружения загружаются при старте
4. **Проверьте спам** - иногда письма попадают туда

### Ошибка "Invalid API key"

- Проверьте, что ключ скопирован полностью (начинается с `re_`)
- Убедитесь, что нет лишних пробелов в `.env.local`
- Перезапустите сервер

### Ошибка "Domain not verified"

- Используйте `onboarding@resend.dev` для теста
- Или настройте свой домен (см. выше)

---

## 📊 Лимиты Resend

- **Бесплатный тариф**: 3000 писем/месяц
- **Бесплатный тариф**: 100 писем/день
- Этого более чем достаточно для начала!

---

## 🔐 Безопасность

⚠️ **ВАЖНО**:
- Никогда не коммитьте `.env.local` в Git (он уже в `.gitignore`)
- Не публикуйте API ключ
- Если ключ скомпрометирован - удалите его в Resend и создайте новый

