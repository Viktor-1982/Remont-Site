# Автоматическая отправка уведомлений подписчикам

## Описание

Система автоматически находит новые статьи (опубликованные за последние 24 часа) и отправляет email-уведомления всем подписчикам на соответствующем языке.

## Что было сделано

1. ✅ Используется существующая таблица `sent_notifications` для отслеживания отправленных уведомлений
2. ✅ Добавлена функция автоматического поиска новых статей
3. ✅ Реализована защита от повторной отправки уведомлений
4. ✅ Настроен GET endpoint для автоматического вызова через cron
5. ✅ Создан `vercel.json` для настройки cron jobs

## Шаг 1: Проверка таблицы в Supabase

Таблица `sent_notifications` уже должна существовать в Supabase. Если её нет, выполните SQL скрипт из файла `supabase/sent-notifications.sql` в Supabase SQL Editor.

Убедитесь, что таблица имеет следующую структуру:
- `id` (uuid, primary key)
- `post_slug` (text, not null)
- `post_locale` (text, not null)
- `sent_at` (timestamptz, not null)
- `subscribers_count` (integer, not null)
- `success_count` (integer, not null)
- `failed_count` (integer, not null)
- Уникальный индекс на `(post_slug, post_locale)`

## Шаг 2: Настройка переменных окружения

Убедитесь, что в Vercel Environment Variables установлены:

- `CRON_SECRET` - секретный ключ для защиты cron jobs (Vercel автоматически добавляет его в заголовок Authorization)
- `RESEND_API_KEY` - API ключ Resend (уже есть ✅)
- `RESEND_FROM_EMAIL` - email отправителя (уже есть ✅)
- `SUPABASE_URL` - URL вашего Supabase проекта (уже есть ✅)
- `SUPABASE_SERVICE_ROLE_KEY` - Service Role Key из Supabase (уже есть ✅)

**Важно:** Vercel рекомендует использовать `CRON_SECRET` для cron jobs. Код автоматически использует `CRON_SECRET`, если он установлен, иначе `NOTIFY_SECRET` (для обратной совместимости).

## Шаг 3: Настройка Vercel Cron Jobs

### Вариант 1: Через vercel.json (рекомендуется)

Файл `vercel.json` уже создан. Просто добавьте переменную окружения `CRON_SECRET` в Vercel, и Vercel автоматически будет добавлять её в заголовок `Authorization` при каждом вызове cron job.

1. Добавьте в Vercel Environment Variables:
   - Key: `CRON_SECRET`
   - Value: `5a211b9aecf3623750ce59edee9f8b19d58d5756018663ef53985316a48c4f0a` (или сгенерируйте свой)
   - Environments: All Environments

2. Vercel автоматически будет вызывать `/api/notify-subscribers` по расписанию из `vercel.json`

**Примечание:** Заголовки добавлять не нужно - Vercel делает это автоматически с `CRON_SECRET`.

### Вариант 2: Через vercel.json (уже настроен)

Файл `vercel.json` уже создан с базовой конфигурацией. Однако для авторизации нужно добавить заголовок через Vercel Dashboard, так как в `vercel.json` нельзя использовать переменные окружения для секретов.

## Шаг 4: Тестирование

### Ручной тест через GET запрос

```bash
curl "https://renohacks.com/api/notify-subscribers?secret=YOUR_NOTIFY_SECRET" \
  -H "Authorization: Bearer YOUR_NOTIFY_SECRET"
```

### Ручной тест через POST запрос (автоматический режим)

```bash
curl -X POST https://renohacks.com/api/notify-subscribers \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NOTIFY_SECRET" \
  -d '{"auto": true}'
```

### Ручной тест для конкретной статьи

```bash
curl -X POST https://renohacks.com/api/notify-subscribers \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_NOTIFY_SECRET" \
  -d '{"slug": "remont-staroy-kvartiry", "locale": "ru"}'
```

## Как это работает

1. **Cron Job** вызывает `GET /api/notify-subscribers` каждые 6 часов (или по вашему расписанию)
2. **Endpoint** находит все статьи, опубликованные за последние 24 часа
3. **Проверка** - для каждой статьи проверяется, не было ли уже отправлено уведомление
4. **Отправка** - если статья новая и не была отправлена, отправляются уведомления всем подписчикам на соответствующем языке
5. **Сохранение** - информация об отправке сохраняется в таблице `sent_notifications`

## Мониторинг

### Проверка отправленных уведомлений в Supabase

```sql
SELECT * FROM sent_notifications 
ORDER BY sent_at DESC 
LIMIT 10;
```

### Логи в Vercel

Перейдите в Vercel Dashboard → ваш проект → **Deployments** → выберите деплой → **View Function Logs**

## Настройка времени проверки новых статей

По умолчанию система ищет статьи, опубликованные за последние **24 часа**. Чтобы изменить это значение, отредактируйте константу `NEW_POST_HOURS` в файле `src/app/api/notify-subscribers/route.ts`:

```typescript
const NEW_POST_HOURS = 24 // измените на нужное значение
```

## Важные замечания

1. **Повторная отправка исключена** - система проверяет таблицу `sent_notifications` и не отправляет уведомления повторно для одной и той же статьи
2. **Языковая фильтрация** - уведомления отправляются только подписчикам, выбравшим язык статьи
3. **Батчинг** - письма отправляются батчами по 50 штук с задержкой 1 секунда между батчами
4. **Лимиты Resend** - убедитесь, что у вас достаточно лимитов в Resend для отправки всех писем

## Устранение проблем

### Уведомления не отправляются

1. Проверьте логи в Vercel
2. Убедитесь, что таблица `sent_notifications` создана в Supabase
3. Проверьте, что все переменные окружения установлены
4. Убедитесь, что `NOTIFY_SECRET` совпадает в переменных окружения и в заголовке cron job

### Повторная отправка уведомлений

Если нужно повторно отправить уведомление для статьи (например, для тестирования), удалите запись из таблицы:

```sql
DELETE FROM sent_notifications 
WHERE post_slug = 'your-article-slug' 
AND post_locale = 'ru';
```

## Готово! 🎉

После настройки система будет автоматически отправлять уведомления подписчикам о новых статьях без вашего участия.

