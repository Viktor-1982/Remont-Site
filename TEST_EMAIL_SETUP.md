# Проверка настройки почты и CRON_SECRET

## ✅ Проверка ключа CRON_SECRET

Ваш ключ: `1546b0bca866f470db34e7896c66f136c873522f2d3f48dc09ba0a8995b6f90f`

**Проверьте в Vercel:**
1. Vercel Dashboard → Settings → Environment Variables
2. Найдите `CRON_SECRET`
3. Убедитесь, что значение совпадает с ключом выше

## 📧 Проверка настройки почты

### Шаг 1: Проверьте переменные окружения в Vercel

Убедитесь, что установлены:
- ✅ `RESEND_API_KEY` - должен начинаться с `re_`
- ✅ `RESEND_FROM_EMAIL` - например, `onboarding@resend.dev` или `noreply@renohacks.com`
- ✅ `CRON_SECRET` - ваш ключ выше

### Шаг 2: Тест подписки (проверка базовой отправки)

1. Откройте сайт: `https://renohacks.com`
2. Подпишитесь на рассылку (введите свой email)
3. Проверьте почту - должно прийти приветственное письмо

Если письмо пришло - почта настроена правильно ✅

### Шаг 3: Тест endpoint для уведомлений

#### Вариант 1: Тест через GET (автоматический поиск новых статей)

```bash
curl "https://renohacks.com/api/notify-subscribers?secret=1546b0bca866f470db34e7896c66f136c873522f2d3f48dc09ba0a8995b6f90f" \
  -H "Authorization: Bearer 1546b0bca866f470db34e7896c66f136c873522f2d3f48dc09ba0a8995b6f90f"
```

#### Вариант 2: Тест через POST (автоматический режим)

```bash
curl -X POST "https://renohacks.com/api/notify-subscribers" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 1546b0bca866f470db34e7896c66f136c873522f2d3f48dc09ba0a8995b6f90f" \
  -d '{"auto": true}'
```

#### Вариант 3: Тест для конкретной статьи

```bash
curl -X POST "https://renohacks.com/api/notify-subscribers" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 1546b0bca866f470db34e7896c66f136c873522f2d3f48dc09ba0a8995b6f90f" \
  -d '{"slug": "remont-staroy-kvartiry", "locale": "ru"}'
```

### Шаг 4: Проверка логов

После теста проверьте логи в Vercel:
1. Vercel Dashboard → Deployments → выберите деплой
2. View Function Logs
3. Ищите вызовы `/api/notify-subscribers`

### Шаг 5: Проверка в Resend Dashboard

1. Откройте [resend.com](https://resend.com) → Dashboard
2. Перейдите в **Logs** или **Emails**
3. Вы должны увидеть отправленные письма

## 🔍 Что проверить, если не работает

### Проблема: Письма не отправляются

1. **Проверьте RESEND_API_KEY:**
   - Должен начинаться с `re_`
   - Должен быть действительным (проверьте в Resend Dashboard)

2. **Проверьте RESEND_FROM_EMAIL:**
   - Если используете `onboarding@resend.dev` - должно работать сразу
   - Если используете свой домен - проверьте настройку DNS в Resend

3. **Проверьте лимиты Resend:**
   - Бесплатный тариф: 100 писем/день
   - Проверьте в Resend Dashboard → Usage

### Проблема: Endpoint возвращает 401 Unauthorized

1. Проверьте, что `CRON_SECRET` в Vercel совпадает с ключом в запросе
2. Убедитесь, что заголовок `Authorization: Bearer YOUR_KEY` правильный
3. Проверьте, что сделан новый деплой после добавления `CRON_SECRET`

### Проблема: Endpoint возвращает "No subscribers found"

1. Проверьте таблицу `subscriptions` в Supabase:
   ```sql
   SELECT * FROM subscriptions;
   ```
2. Убедитесь, что есть подписчики с нужным языком (`ru` или `en`)

## ✅ Успешный ответ от endpoint

Если всё работает правильно, вы получите ответ вида:

```json
{
  "message": "Processed 1 new posts",
  "processed": 1,
  "totalSent": 150,
  "totalFailed": 0,
  "results": [
    {
      "slug": "remont-staroy-kvartiry",
      "locale": "ru",
      "title": "Ремонт старой квартиры: системный подход без ошибок",
      "sent": 150,
      "failed": 0,
      "total": 150
    }
  ]
}
```

## 🎯 Быстрая проверка всего

Выполните этот тест - он проверит всё сразу:

```bash
# 1. Тест подписки (базовая отправка)
# Подпишитесь на сайте и проверьте почту

# 2. Тест endpoint (автоматические уведомления)
curl -X POST "https://renohacks.com/api/notify-subscribers" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 1546b0bca866f470db34e7896c66f136c873522f2d3f48dc09ba0a8995b6f90f" \
  -d '{"auto": true}'
```

Если оба теста прошли успешно - всё настроено правильно! ✅

