# 🚀 Простая настройка Resend (без DNS)

## ✅ Да, можно использовать Resend БЕЗ настройки DNS!

Resend предоставляет тестовый домен `onboarding@resend.dev`, который работает сразу без настройки.

---

## Пошаговая настройка (5 минут)

### Шаг 1: Регистрация в Resend

1. Откройте [resend.com](https://resend.com)
2. Нажмите **"Sign Up"** (можно через GitHub)
3. Подтвердите email

### Шаг 2: Создание API ключа

1. В дашборде: **API Keys** → **Create API Key**
2. Имя: `Renohacks Newsletter`
3. Права: **Sending access** ✅
4. Скопируйте ключ (формат: `re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)

### Шаг 3: Настройка проекта

Создайте файл `.env.local` в корне проекта:

```env
RESEND_API_KEY=re_ваш-ключ-из-шага-2
RESEND_FROM_EMAIL=onboarding@resend.dev
```

**Вот и всё!** Никаких DNS записей не нужно.

### Шаг 4: Перезапуск

```bash
npm run dev
```

### Шаг 5: Тест

1. Откройте сайт
2. Подпишитесь на рассылку
3. Проверьте почту - письмо придет с адреса `onboarding@resend.dev`

---

## ✅ Преимущества этого способа

- ✅ Работает сразу, без настройки DNS
- ✅ Не нужно трогать Namecheap
- ✅ Идеально для тестирования
- ✅ Можно использовать в продакшене

---

## ⚠️ Ограничения

- Письма приходят с `onboarding@resend.dev` (не с вашего домена)
- Лимит: 100 писем/день на бесплатном тарифе
- Может чаще попадать в спам (но обычно работает нормально)

---

## 📧 Для продакшена (Vercel)

1. Откройте проект на [vercel.com](https://vercel.com)
2. **Settings** → **Environment Variables**
3. Добавьте:
   - `RESEND_API_KEY` = `re_ваш-ключ`
   - `RESEND_FROM_EMAIL` = `onboarding@resend.dev`
4. **Save**
5. Перезапустите деплой

---

## 🔄 Позже можно перейти на свой домен

Если захотите использовать `noreply@renohacks.com`:
1. Настройте DNS записи в Namecheap (как в инструкции выше)
2. Измените `RESEND_FROM_EMAIL` на `noreply@renohacks.com`
3. Готово!

Но для начала `onboarding@resend.dev` отлично подойдет!

