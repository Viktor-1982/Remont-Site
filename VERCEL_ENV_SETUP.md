# 🚀 Настройка переменных окружения в Vercel

## Пошаговая инструкция

### Шаг 1: Откройте проект в Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите в аккаунт
3. Найдите ваш проект `repair-blog` (или как он называется)
4. Нажмите на проект

### Шаг 2: Перейдите в настройки

1. В верхнем меню проекта нажмите **"Settings"** (Настройки)
2. В левом меню выберите **"Environment Variables"** (Переменные окружения)

### Шаг 3: Добавьте первую переменную

1. В поле **"Key"** введите: `RESEND_API_KEY`
2. В поле **"Value"** введите: `re_Wy8cR537_Jr2dDqAwC5AkEfBrubngAkcw`
3. Выберите **Environment** (окружения):
   - ✅ **Production** (для продакшена)
   - ✅ **Preview** (для preview деплоев)
   - ✅ **Development** (опционально)
4. Нажмите **"Add"** или **"Save"**

### Шаг 4: Добавьте вторую переменную

1. В поле **"Key"** введите: `RESEND_FROM_EMAIL`
2. В поле **"Value"** введите: `onboarding@resend.dev`
3. Выберите **Environment**:
   - ✅ **Production**
   - ✅ **Preview**
   - ✅ **Development** (опционально)
4. Нажмите **"Add"** или **"Save"**

### Шаг 5: Перезапустите деплой

1. Перейдите на вкладку **"Deployments"** (Деплои)
2. Найдите последний деплой
3. Нажмите на **"..."** (три точки) справа от деплоя
4. Выберите **"Redeploy"** (Переразвернуть)
5. Подтвердите переразвертывание

**Или:**

1. Сделайте любой коммит и пуш в Git
2. Vercel автоматически создаст новый деплой с новыми переменными

---

## ✅ Проверка

После переразвертывания:

1. Откройте ваш сайт на продакшене
2. Подпишитесь на рассылку
3. Проверьте почту - письмо должно прийти

---

## 📸 Визуальная инструкция

### Где найти Settings:
```
Dashboard → [Ваш проект] → Settings (вверху)
```

### Где Environment Variables:
```
Settings → Environment Variables (в левом меню)
```

### Как добавить переменную:
```
Key: RESEND_API_KEY
Value: re_Wy8cR537_Jr2dDqAwC5AkEfBrubngAkcw
Environment: ☑ Production ☑ Preview ☑ Development
[Add]
```

---

## ⚠️ Важно

- **Не коммитьте** `.env.local` в Git (он уже в `.gitignore`)
- Переменные в Vercel **безопасны** - они не видны в коде
- После добавления переменных **обязательно** перезапустите деплой
- Переменные применяются только к **новым** деплоям

---

## 🔍 Проверка переменных

После деплоя можно проверить, что переменные загружены:

1. В Vercel: **Deployments** → выберите деплой → **"View Function Logs"**
2. В логах не должно быть ошибок о `RESEND_API_KEY`

---

## ✅ Готово!

Теперь email-рассылка будет работать и на продакшене!

